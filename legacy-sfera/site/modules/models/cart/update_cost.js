/*
self.update_cost = function (data) {
    for (var i = 0; i < self.cart().items().length; i++) {
        if (self.cart().items()[i] == data) {
            let price = Items[data.guid()].cost;
            self.cart().items()[i].product_price(price);
        }
    };
};*/
